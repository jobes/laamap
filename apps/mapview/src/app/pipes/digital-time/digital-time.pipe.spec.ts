import { DigitalTimePipe } from './digital-time.pipe';

describe('DigitalTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DigitalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
